/*
 * Configuração alternativa em tempo de execução.
 *
 * Recomendado no Netlify: defina a variável de ambiente VITE_GAME_URL.
 * Como alternativa, preencha gameUrl abaixo antes de publicar a landing.
 */
window.DETAILER_CONFIG = {
  gameUrl: '',
};
